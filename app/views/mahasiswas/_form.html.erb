  <%= form_with(model: mahasiswa, local: true) do |form| %>
  <% if mahasiswa.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(mahasiswa.errors.count, "error") %> prohibited this mahasiswa from being saved:</h2>

      <ul>
      <% mahasiswa.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form group">
    <%= form.label :nama %>
    <%= form.text_field :nama, class: "form form-control" %>
  </div>

  <div class="form group">
    <%= form.label :alamat %>
    <%= form.text_field :alamat, class: "form form-control" %>
  </div>

  <div class="form group">
    <%= form.label :tanggal %>
    <%= form.date_select :tanggal %>
  </div>

  <div class="actions">
    <%= form.submit class:"btn btn-primary" %>
  </div>
<% end %>
